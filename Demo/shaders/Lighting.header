struct SDirectionalLight
{
	float _fIlluminance;
	vec3 _vDirection;
};

float GetDirectionalLight(SDirectionalLight l, vec3 N)
{	
	vec3 L = normalize(l._vDirection);
	float NdotL = clamp(dot(N,L), 0.f, 1.f);
	return l._fIlluminance * NdotL;
}